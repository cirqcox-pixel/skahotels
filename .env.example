# SKA Hotels environment (copy to .env — never commit .env)

# Database driver: mysql | supabase
DB_CONNECTION=mysql

# MySQL (local / cPanel — default, backward compatible)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=skabcwvw_ska001
DB_USERNAME=your_mysql_user
DB_PASSWORD=your_mysql_password

# Supabase (GitHub Pages + optional PHP PostgREST)
SUPABASE_URL=https://nllqkepymtwwbvbjnbyz.supabase.co
# Publishable/anon key from Project Settings → API (safe to commit in ska-config.js)
SUPABASE_ANON_KEY=
# Service role key — server-side PHP ONLY, never expose in frontend repo
SUPABASE_SERVICE_ROLE_KEY=

# Site
SITE_URL=https://cirqcox-pixel.github.io/skahotels
